# Database
DATABASE_URL="mysql://user:password@localhost:3307/revlosport_kb"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# API Key untuk chatbot mengakses /api/knowledge-base
KB_API_KEY="your-secret-api-key-here"

# Internal automation key — dipakai n8n untuk memanggil /api/internal/* endpoints
# Generate dengan: openssl rand -hex 32
INTERNAL_AUTOMATION_KEY="generate-with-openssl-rand-hex-32"
